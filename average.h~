#include <stdio.h>

int average(int payments[])
{
	int average;
	int payments_size = sizeof(payments);
	int i;
	int sum =0;

	for(i = 0; i < payments_size; i++){
		 sum = sum + payments[i];
	}

	average = sum / payments_size;

	return average;
}
